{% extends 'base.html.twig' %}

{% block title %}Avantage{% endblock %}

{% block body %}
    <h1>Avantage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ avantage.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ avantage.description }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ avantage.titre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_avantage_index') }}">back to list</a>

    <a href="{{ path('app_avantage_edit', {'id': avantage.id}) }}">edit</a>

    {{ include('avantage/_delete_form.html.twig') }}
{% endblock %}
