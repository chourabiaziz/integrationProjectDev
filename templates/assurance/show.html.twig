{% extends 'base.html.twig' %}

{% block title %}Assurance{% endblock %}

{% block body %}
    <h1>Assurance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ assurance.id }}</td>
            </tr>
            <tr>
                <th>NomAssurance</th>
                <td>{{ assurance.NomAssurance }}</td>
            </tr>
            <tr>
                <th>AdresseAssurance</th>
                <td>{{ assurance.adresseAssurance }}</td>
            </tr>
            <tr>
                <th>CodePostalAssurance</th>
                <td>{{ assurance.codePostalAssurance }}</td>
            </tr>
            <tr>
                <th>TelAssurance</th>
                <td>{{ assurance.telAssurance }}</td>
            </tr>
            <tr>
                <th>EmailAssurance</th>
                <td>{{ assurance.emailAssurance }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_assurance_index') }}">back to list</a>

    <a href="{{ path('app_assurance_edit', {'id': assurance.id}) }}">edit</a>

    {{ include('assurance/_delete_form.html.twig') }}
{% endblock %}
