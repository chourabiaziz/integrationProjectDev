{% extends 'base.html.twig' %}

{% block title %}Edit Assurance{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="card">
            <div class="card-header bg-primary text-white">
                <h1 class="card-title">Edit Assurance</h1>
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-md-8">
                        {{ include('assurance/_form.html.twig', {'button_label': 'Update'}) }}
                    </div>
                </div>
            </div>
            <div class="card-footer">
                <div class="row">
                    <div class="col-md-8">
                        <a href="{{ path('app_assurance_index') }}" class="btn btn-secondary">Retour à la liste</a>
                        <button type="submit" class="btn btn-primary">Enregistrer</button>
                    </div>
                </div>
                <div class="row mt-3">
                    <div class="col-md-8">
                        {{ include('assurance/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
