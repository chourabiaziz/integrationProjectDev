{% extends 'base.html.twig' %}

{% block title %}Edit Constat{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Edit Constat</h1>

        <div class="row">
            <div class="col-md-8">
                {{ include('constat/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
        </div>

        <div class="row mt-3">
            <div class="col-md-8">
                <a href="{{ path('app_constat_index') }}" class="btn btn-secondary">Back to List</a>
            </div>
        </div>

        <div class="row mt-3">
            <div class="col-md-8">
                {{ include('constat/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
